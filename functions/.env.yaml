# Environment variables for Cloud Functions deployment
# These are injected at deploy time via --env-vars-file
METACLAW_USER_ID: "default_user"
DATABASE_URL: "postgresql://user:pass@host/dbname" # TODO: Update this with the real Neon PostgreSQL connection string
